# Create Spring Boot project
mvn spring-boot:run

# Setup PostgreSQL (Docker)
docker run --name citybus-postgres -e POSTGRES_DB=citybus -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=password -p 5432:5432 -d postgres:13
